# Mobile Panchangam Image Share Implementation Plan (v2 - Traditional & Complete)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Allow users on mobile and desktop devices to share the complete daily Panchangam details as a single, high-resolution PNG image card in a traditional Tamil daily wall calendar design, 100% localized to the selected language.

**Architecture:** Redesign `PanchangamShareCard.jsx` with a traditional wall calendar theme (red/saffron banner `#b71c1c`, parchment background `#fffdf8`, double border frame) including all missing sections (Karanam, Nakshatra Yogam, Abhijit Muhurtham, 24 Horais). Use `t(key, lang)` for dynamic multilingual rendering.

**Tech Stack:** React 19, `html2canvas`, Web Share API (`navigator.share`), `t()` translation helper.

## Global Constraints
- Target File 1: `frontend/src/components/PanchangamShareCard.jsx`
- Target File 2: `frontend/src/pages/PanchangamPage.jsx`
- Footer Branding Verbatim: `Generated by https://tinyurl.com/drik-vedic`

---

### Task 1: Redesign `PanchangamShareCard.jsx` with Traditional Wall Calendar Aesthetic & Full Localized Content

**Files:**
- Modify: `frontend/src/components/PanchangamShareCard.jsx`

**Interfaces:**
- Consumes: `data` (DailyPanchangamDTO object), `currentDate` (string), `settings` (language, location object), `t` (translation helper)
- Produces: Hidden 1080px DOM node styled as a Traditional Wall Calendar in the selected language

- [ ] **Step 1: Write updated `PanchangamShareCard.jsx` with complete fields and multilingual support**

- [ ] **Step 2: Compile & verify React build**

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/PanchangamShareCard.jsx
git commit -m "feat: redesign PanchangamShareCard with Traditional Wall Calendar theme and full localized fields"
```

---

### Task 2: Verify & Restart Spring Boot Server

**Files:**
- Modify: None (build verification & server restart)

- [ ] **Step 1: Run `mvn compile` to build target package**

- [ ] **Step 2: Restart Spring Boot server**

- [ ] **Step 3: Commit and push**
